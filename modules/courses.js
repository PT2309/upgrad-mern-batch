let courses = ['react', 'java', 'node.js', 'HTML/CSS'];
let duration = [10, 20, 20, 10];

let myFunc = () => console.log('Welcome');

console.log(courses);

module.exports = {
    // ES6 
    courses,
    duration: duration,
    myFunc
};

