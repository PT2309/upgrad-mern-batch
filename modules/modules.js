let { courses, duration, myFunc } = require('./courses');
let testing = require('../testing node/testing');

// let obj =  { name: 'john', age: 12}

// let { age } = obj;
// console.log(age);
console.log('Name of the courses', courses);
console.log('Durations of the courses', duration);
myFunc();